# Agent-to-Agent Artifact Index

Cycle **cycle-002 "VUX v1 Strategic Treasury"** — local sprint IDs equal global IDs (1:1).

| sprint | theme | implementation | review | audit | status |
|---|---|---|---|---|---|
| sprint-1 | Provenance-gated foundation & authorized vendoring | [`sprint-1/reviewer.md`](sprint-1/reviewer.md) (remediated 2026-08-11) | [`sprint-1/engineer-feedback.md`](sprint-1/engineer-feedback.md) — **APPROVED** (re-review, 8/8 AC) | [`sprint-1/auditor-sprint-feedback.md`](sprint-1/auditor-sprint-feedback.md) — **APPROVED** (0 critical / 0 high / 0 medium / 4 low) | `LANDED_VERIFIED` — operator-accepted and landed on `master` (`23263e18`) |
| sprint-2 | VUX token & Hard Reserve | [`sprint-2/reviewer.md`](sprint-2/reviewer.md) (A-1 remediated 2026-08-11) | [`sprint-2/engineer-feedback.md`](sprint-2/engineer-feedback.md) — **APPROVED** (pass 2, A-1 re-review: 0 critical / 0 high / 1 medium / 2 low) on subject `a6313a4d5a…2b772cf`; pass 1 (7/7 AC) preserved in-file | [`sprint-2/auditor-sprint-feedback.md`](sprint-2/auditor-sprint-feedback.md) — **APPROVED** (pass 2, exact-tree re-audit: 0 critical / 0 high / **1 medium: M-1 (non-blocking, binding pre-Sprint-3 condition)** / 5 low; A-1 CLOSED) on subject `a6313a4d5a…2b772cf` (entry == exit); pass 1 preserved in-file | `LANDED_VERIFIED` — operator-accepted 2026-08-12 at digest `a6313a4d5a…2b772cf` and landed on `master` (`89a92055`); committed-tree digest re-verified byte-identical to the accepted subject |
| sprint-3 | Rig: throne, settlement, VEM + invariant suite | [`sprint-3/reviewer.md`](sprint-3/reviewer.md) (8/8 AC, validator exit 0 on the scoped slice) | [`sprint-3/engineer-feedback.md`](sprint-3/engineer-feedback.md) — **APPROVED** (8/8 AC re-derived on the exact tree: 0 critical / 0 high / 0 medium / 6 low + 3 informational) on subject `src/Rig.sol` `f0377bf9…8ac2b1e` / `foundry.toml` `74d5be36…049e6d387`; `via_ir` compiler-mode change dispositioned in §8 | [`sprint-3/auditor-sprint-feedback.md`](sprint-3/auditor-sprint-feedback.md) — **APPROVED** (exact-tree audit: 0 critical / 0 high / 0 medium / 8 low) on subject `src/Rig.sol` `f0377bf9…8ac2b1e` (entry == exit); 8 LOW findings carried forward as non-blocking (`evm_version`/`decayFloor`/`treasury!=reserve` deferred to Sprint 7/8, remainder documentation) | `OPERATOR_ACCEPTED_READY_TO_LAND` — operator accepted 2026-08-13; `COMPLETED` marker written (`sprint-3/COMPLETED`); uncommitted on branch `sprint-3`, baseline `bc5dedc2`; `sprint.md` unchanged since review approval (`bcaebd18…abaa5557`); landing (branch → `master`) deferred to the dedicated landing node |
| sprint-4 | Strategic Treasury I: custody, classification, authority | [`sprint-4/reviewer.md`](sprint-4/reviewer.md) (9/9 AC, validator exit 0 on the scoped slice) | [`sprint-4/engineer-feedback.md`](sprint-4/engineer-feedback.md) — **APPROVED** (9/9 AC re-derived on the exact tree: 0 critical / 0 high / 0 medium / 4 low + 3 informational) on subject `72896e9d…75d3966b` (24 files, fingerprint recomputed and proven exhaustive); **J-3** dispositioned in §4 (accumulator model is accepted authority — the stronger rule would have resolved operator-reserved **R-9** and implemented future waterfall doctrine at P0, both forbidden), **J-1** dispositioned in §8 (resolved `[profile.v3core]` settings verified, frozen artifact emitted, hash byte-identical — non-blocking) | [`sprint-4/auditor-sprint-feedback.md`](sprint-4/auditor-sprint-feedback.md) — **APPROVED** (exact-tree adversarial audit: 0 critical / 0 high / 0 medium / 4 low + 5 informational) on subject `72896e9d…75d3966b` (24 files; fingerprint and exhaustiveness re-established at node **entry and exit**). Core isolation proven on **three independent legs** (`metadata.sources`; absence of `assembly`/`delegatecall`/low-level `call`/`approve`; controls-validated shift-aware runtime scan — 10/10 controls found, 9/9 dangerous selectors absent). **J-3** disposition: implementation is **not stronger** than the accepted lifetime-credit model (§4). **Permissionless `returnFor`** misattribution confirmed reachable without operator action and graded independently as the accepted SDD §1.10 rule-4 "≤ theft" class, disclosed by `ReturnedFromStrategy` (§5). New **LOW-1**: `recallFromStrategy` omits the `UnknownReturnAsset` guard (defense-in-depth; no laundering path, PoC-verified). 14/14 independent adversarial PoCs pass | `OPERATOR_ACCEPTED_READY_TO_LAND` — operator accepted 2026-08-13 at fingerprint `72896e9d…75d3966b` (24 files, exact audited tree preserved byte-for-byte); `COMPLETED` marker written (`sprint-4/COMPLETED`); uncommitted on branch `sprint-4`, baseline `84abced4`; `sprint.md` unchanged since review approval (v1.1.1, 22 Sprint-4 checkboxes ticked); `ledger.json` sprint-4 `planned` → `completed` (repaired — byte-identical through implementation/review/audit, all nine Beads tasks already `closed`, native updater blocked by a pre-existing BusyBox `flock` lock, no task completion falsified); landing (branch → `master`) deferred to the dedicated landing node |
| sprint-5 | Strategic Treasury II: POL, callbacks, VYRF | [`sprint-5/reviewer.md`](sprint-5/reviewer.md) (8/8 AC, validator exit 0 on the scoped slice; validator discrimination confirmed by a removed-section negative control) | [`sprint-5/engineer-feedback.md`](sprint-5/engineer-feedback.md) — **APPROVED** (8/8 AC re-derived on the exact tree: 0 critical / 0 high / 0 medium / 2 low) on subject `37fa6943…1d06475a` (12 files, fingerprint recomputed and proven exhaustive against an 8-mutation discrimination battery); **J-1** dispositioned in §7 (`buyVuxForPol`'s VUX purchase books as POL inventory principal without incrementing `polVuxPrincipal` — accepted as the no-double-count reading of sdd.md:L140's fixed cell set, not a third basis cell); raises **L-1** (consume-before-pay ordering implemented but not exercised by any test) and **L-2** (tautological assertion at `TreasuryPol.t.sol:338`) as disclosed context | [`sprint-5/auditor-sprint-feedback.md`](sprint-5/auditor-sprint-feedback.md) — **APPROVED** (exact-tree audit: 0 critical / 0 high / 0 medium / 3 low + 2 informational + 1 process observation + 1 carried residual) on subject `37fa6943…1d06475a` (entry == exit, 12 files); review findings L-1 and L-2 independently affirmed as LOW; new **A-1** (LOW, non-blocking, PoC-confirmed): `decreasePol`'s fee-disposal external calls sit inside the fee-collect → principal-burn window while the pool is unlocked, unreachable in the accepted deployment (VUX and canonical WETH are both hook-free) and carried as bounded hardening; **P-1** process observation notes the review artifact initially failed `verdict-derive.sh --require-trailer` (non-substantive trailer-position issue, re-run exits 0) | `LANDED_VERIFIED` — operator-accepted 2026-08-14 at fingerprint `37fa6943…1d06475a` (12 files, exact-tree identity re-derived from git at review entry, audit entry, audit exit, **and** acceptance — unchanged throughout) and landed on `master` (commit hash recorded in a follow-up chore commit, per sprint-1/sprint-2 precedent); accepted residuals carried forward without code change: review **L-1**/**L-2** (non-blocking LOWs), audit **A-1** (non-blocking LOW hardening item under the accepted current-WETH trust model), **R-1** (carried external/fork residual owned by Sprints 7/8), **P-1** (closed process observation); `COMPLETED` marker preserved (`sprint-5/COMPLETED`, written by `/audit-sprint sprint-5`); `sprint.md` Sprint-5 checkboxes ticked (18 boxes: 4 deliverables + 8 acceptance criteria + 6 technical tasks); `ledger.json` sprint-5 `planned` → `completed`; 349/349 tests re-verified clean at acceptance under default **and** `[profile.ci]` depth |
| sprint-6 | Truth surfaces: Lens, indexer, truthful UX | — | — | — | planned |
| sprint-7 | Genesis: non-griefable launch & adversarial rehearsal | — | — | — | planned |
| sprint-8 | Launch readiness: hardening, traceability, E2E | — | — | — | planned |

## sprint-1 artifacts

| file | role |
|---|---|
| `sprint-1/reviewer.md` | implementation report + AC verification table — refreshed at the remediation node (finding dispositions, negative-test evidence, 8/8 ACs) |
| `sprint-1/sprint-1-scope.md` | byte-exact Sprint-1 slice of `sprint.md`, the AC validator's scoped input (`0133a5b8…799aa`) — the **pre-approval** slice; `sprint.md`'s hash changed when the Sprint-1 checkboxes were ticked on approval, criteria text unchanged |
| `sprint-1/engineer-feedback.md` | review verdict — **APPROVED** at the second pass (C-1 + all 4 MEDIUM closed, verified by reviewer-controlled probes); 5 non-blocking hardening findings N-1…N-5 carried to audit as disclosed context |

## sprint-2 artifacts

| file | role |
|---|---|
| `sprint-2/reviewer.md` | implementation report + AC verification table (7/7 ACs, validator exit 0), with the appended **A-1 remediation pass** section (root cause, exact universe fix, 4 mixed-case negative probes, compiler-evidence build-reachability control, full regression, subject digests) |
| `sprint-2/sprint-2-scope.md` | byte-exact Sprint-2 slice of `sprint.md`, the AC validator's scoped input (`c5060bff…4c8d508`) |
| `sprint-2/evidence/prov-5-similarity-review.md` | PROV-5 clean-source statement + post-hoc structural similarity assessment for `HardReserve.sol` (AC-7) |
| `sprint-2/evidence/structural-absence-checklist.md` | FR-7.2 / FR-7.3 / INV-14 / INV-5 row-by-row checklist, each row naming the mechanical artifact that would fail (AC-4) |
| `sprint-2/engineer-feedback.md` | review verdict — **APPROVED**. Pass 2 (A-1 re-review) prepended, pass 1 preserved verbatim below the divider. Pass 2 independently recovered the subject-digest convention, byte-exactly reconstructed the `provenance.yml` and `verify-census.sh` audit-entry pre-images, ran 6 reviewer-authored mixed-case probes and 2 proof-of-mutation falsification tests, and reproduced the build-reachability control from compiler evidence. Raises **M-1** (extension-axis residual, MEDIUM, pre-existing and outside A-1 scope) + L-3, L-4, I-1, I-2 as disclosed context |
| `sprint-2/auditor-sprint-feedback.md` | audit verdict — **APPROVED** with 1 MEDIUM (**A-1**: case-sensitive source-universe walk, evadable by extension case) + 3 LOW (R-1, L-1, L-2); entry == exit subject digest `78c8881204…2ac45a`. Contains the addendum retracting the sprint-1 **N-2** refutation. **Preserved as written** — the remediation is recorded in `reviewer.md`, not by rewriting this artifact |

## sprint-3 artifacts

| file | role |
|---|---|
| `sprint-3/reviewer.md` | implementation report + AC verification table (8/8 ACs, validator exit 0 on the scoped slice); §6 records the two build-configuration judgment calls (`via_ir` enablement and the v3core inheritance leak it caused, caught by the `POOL_INIT_CODE_HASH` gate) |
| `sprint-3/sprint-3-scope.md` | byte-exact Sprint-3 slice of `sprint.md` L236–L290, the AC validator's scoped input (`1584e2e1…e8a815b6`) — the **pre-approval** slice, verified byte-exact at review entry; `sprint.md`'s hash changed when the 19 Sprint-3 checkboxes were ticked on approval, criteria text unchanged (same status as `sprint-1-scope.md`/`sprint-2-scope.md`) |
| `sprint-3/engineer-feedback.md` | review verdict — **APPROVED**. Every load-bearing claim re-derived on the exact tree rather than accepted on report: all 16 subject-manifest hashes re-computed, `_route`/`_vem` checked term-for-term against prd.md:L368/L386, 144/144 tests and CI-depth (fuzz 10,000 / invariant 16,384 calls, 0 reverts) re-run, Rig surface enumerated from the artifact (26 entries, `take` the only mutator), all four prohibited-signal commands reproduced. §8 is the standalone `via_ir` disposition: necessity proven by reproducing `Stack too deep` under both legacy configurations, Sprint-2 `VUX`/`HardReserve` runtime-bytecode deltas measured against a `bc5dedc2` worktree build, and the accumulated evidence shown sufficient because it is live-regenerated from `out/` with positive controls; frozen v3-core creation bytecode confirmed byte-identical (`0df5293b…`). Carries L-1…L-6 + I-1…I-3 as disclosed context |
| `sprint-3/evidence/subject-manifest.md` | SHA-256 manifest separating (A) the Sprint-3 implementation subject, (B) this node's lifecycle evidence, and (C) pre-existing State Zone material left untouched |
| `sprint-3/evidence/prohibited-signal-inspection.md` | FR-4.3 named checklist (AC-7) — all 14 primary-path branches enumerated; the `pure` modifier on both formulas and the four-`immutable` reference graph as the structural argument |
| `sprint-3/evidence/prov-3-similarity-review.md` | PROV-3 clean-source statement — per-section split of the Miner-derived skeleton vs. the VUX-original monetary surfaces, plus the sources-not-consulted list |
| `sprint-3/evidence/fb-1-mining-redemption-independence.md` | FB-1 review note (review-assigned row) — redemption's dependency graph contains no reference to the Rig |

## sprint-4 artifacts

| file | role |
|---|---|
| `sprint-4/reviewer.md` | implementation report + AC verification (9/9 ACs, validator exit 0 on the scoped slice). "Judgment Calls" carries eight items for explicit review disposition — **J-1** the `foundry.toml` profile-inheritance hazard (the Sprint-3 residual, hit a second time in a new key), **J-2** the `IStrategyAdapter`/`ILSGRewardProgram` interface shapes, **J-3** `realizedRevenue` as an accumulator rather than a segregated balance, **J-4** `closeStrategy` across all modes, **J-5** `deployMarginalBySignal` filter-then-split, **J-6** `=0.7.6` revert strings, **J-7** the absent `StrategicInflow` class-1 emitter, **J-8** the seven error names outside the accepted §6.1 table |
| `sprint-4/sprint-4-scope.md` | byte-exact Sprint-4 slice of `sprint.md` L292–L350, the AC validator's scoped input (`afde57a6…9497aef9`) — the **pre-approval** slice, verified byte-exact at review entry; `sprint.md`'s hash changed when the 22 Sprint-4 checkboxes were ticked on approval, criteria text unchanged (same status as `sprint-1-scope.md`…`sprint-3-scope.md`) |
| `sprint-4/engineer-feedback.md` | review verdict — **APPROVED** (0 critical / 0 high / 0 medium / 4 low + 3 informational). Every load-bearing claim re-derived on the exact tree rather than accepted on report: subject fingerprint recomputed **and** proven exhaustive against `git status`, PRD/SDD hashes re-hashed and matched, the 44-entry ABI enumerated closed-world from the artifact, 13 role-gated mutators counted mechanically, 298/298 re-run at both depths (14 fuzz suites × 10,000; 16 invariants × 16,384 calls, 0 reverts), 8/8 gates and `POOL_INIT_CODE_HASH` reproduced. §4 is the standalone **J-3 disposition** — resolved *in favour of the implementation* on four converging authorities (SDD:L312's custody-free bound; SDD:L295/§1.10-rule-3 refusing to net losses against revenue; **R-9** reserving realized-loss/high-water restoration to operator execution; FR-12.4 forbidding P0 from implementing it) — **not** HITL, since the two rules bind different authorities rather than conflicting. §8 is the **J-1 disposition**: resolved `[profile.v3core]` settings dumped via `forge config` and matched to the refreeze set, frozen pool artifact confirmed emitted with real bytecode. §5 proves the core-isolation claim at the compilation-unit level from `metadata.sources` |
| `sprint-4/evidence/subject-manifest.md` | SHA-256 manifest separating (A) the 24-file Sprint-4 subject (fingerprint `72896e9d…75d3966b`), (B) this node's lifecycle evidence, and (C) pre-existing State Zone material left untouched; also records the two *modified* files and why each was necessary |
| `sprint-4/evidence/prov-5-similarity-review.md` | PROV-5 clean-source statement — per-file provenance class, the `VuxPoolDeployer` derivation-by-inheritance argument (census unchanged at 63 files, `POOL_INIT_CODE_HASH` unaffected), and the sources-not-consulted list incl. the PROV-4 no-consultation rule |
| `sprint-4/evidence/fraud-vs-theft-argument.md` | Task 4.9 adversarial-adapter argument (sdd.md:L302) — per-member analysis of what a lying adapter achieves, the two guards added because they close cost-free lies, and the sharpest misattribution case stated plainly rather than argued away |
| `sprint-4/evidence/r1-r14-reservation-sweep.md` | R-1…R-14 walked row by row: nothing operator-reserved is resolved, defaulted, or frozen; fixture values enumerated with their `test/`-only scope |
| `sprint-4/evidence/fb-6-9-10-12-scenario-notes.md` | the four review-assigned FB rows, each with the code that makes the canonical outcome true and the mechanical evidence that exists anyway |
| `sprint-4/auditor-sprint-feedback.md` | security audit verdict — **APPROVED** (0 critical / 0 high / 0 medium / 4 low + 5 informational) on subject `72896e9d…75d3966b`, fingerprint and exhaustiveness re-established at node **entry and exit**. §3 derives the classification laws from SDD §1.10 before reading the code and checks all five `realizedRevenue` write sites; §4 tests eight named routes for anything **stronger** than the accepted J-3 model and finds none; §5 grades the permissionless-`returnFor` misattribution economically from the code (attacker surrenders the assets, gains nothing, protocol ends no worse than under theft, and the event discloses it) rather than from the existing narrative; §7 proves core isolation on three independent legs and records a false-positive scan discarded on its own controls; §12 verifies **resolved** profile settings and reproduces `POOL_INIT_CODE_HASH` with an independently written Keccak-256. New findings: **LOW-1** `recallFromStrategy` omits the `UnknownReturnAsset` guard, **LOW-2** `activateLSG` replaces a live module in place against its docstring. No `COMPLETED` marker, ledger, or Beads mutation |
| `sprint-4/evidence/auditor-adversarial-poc.sol.txt` | the audit node's 14 independent adversarial proof-of-concept tests, preserved as `.txt` so they can never be compiled into the subject. Executed against the exact tree (14/14 pass) from a transient `test/audit-poc/` directory that was removed, after which the subject fingerprint and 24-file completeness were both re-verified. Covers misattribution + its event disclosure, the recall-guard asymmetry, laundering-impossibility, accumulator/overflow, hostile-module containment, credit-without-delivery, bare-inflow, write-off, total role takeover, VUX burn-only, earmark bounds, reentrancy, and in-place LSG replacement |

## sprint-5 artifacts

| file | role |
|---|---|
| `sprint-5/reviewer.md` | implementation report + AC verification table (8/8 ACs, validator exit 0 on the scoped slice). Records the E2E observable table from the live-anvil run, the liquidity-inversion direction proof and its **measured** 19-wei/364-wei slack, the fork gap as a named residual rather than a simulated pass, and three judgment calls — **J-1** (`buyVuxForPol` does not increment `polVuxPrincipal`: incrementing on both purchase and deployment would double-count, and sdd.md:L140 fixes the cell set as "exactly these", so there is no third cell for an undeployed-inventory basis), **J-2** (`mintPolPosition`/`increasePol` differ only by a precondition), **J-3** (the closed-world surface array grew 44 → 53) |
| `sprint-5/sprint-5-scope.md` | byte-exact Sprint-5 slice of `sprint.md` (lines 353–407), the AC validator's scoped input (`829f3437…834a7e`) |
| `sprint-5/evidence/subject-manifest.md` | 12-file subject manifest and fingerprint `37fa6943…1d06475a`, **derived from git rather than from expectation** (`R` = all deltas; `A`/`B` a pure path-prefix partition) and proven exhaustive in both directions. Records the two modifications and why each was necessary, that `foundry.toml` is unchanged, and the toolchain-resolved `[profile.v3core]` settings |
| `sprint-5/engineer-feedback.md` | review verdict — **APPROVED** (8/8 AC re-derived on the exact tree: 0 critical / 0 high / 0 medium / 2 low). §7 is the standalone **J-1 disposition** (`buyVuxForPol` books purchased VUX as POL inventory principal without a second `polVuxPrincipal` increment — accepted as the no-double-count reading of sdd.md:L140's fixed cell set); §3 is an 8-mutation discrimination battery proving the AC validator actually discriminates; raises **L-1** (consume-before-pay implemented but untested) and **L-2** (tautological assertion) as disclosed context |
| `sprint-5/auditor-sprint-feedback.md` | security audit verdict — **APPROVED** (0 critical / 0 high / 0 medium / 3 low + 2 informational + 1 process observation + 1 carried residual) on subject `37fa6943…1d06475a`, fingerprint and exhaustiveness re-established at node entry **and** exit. Review's L-1/L-2 independently affirmed; new **A-1** (LOW, PoC-confirmed): `decreasePol`'s fee-disposal external calls sit inside the unlocked fee-collect → principal-burn window, unreachable under the accepted hook-free deployment. §12 states the terminal state plainly: approval establishes eligibility for operator acceptance, not acceptance itself — nothing was committed, pushed, landed, or promoted |

## bounded nodes (not sprints)

| node | theme | implementation | review | audit | status |
|---|---|---|---|---|---|
| `foundry-v1.5-refreeze` | Foundry v1.0.0 → v1.5.0 toolchain refreeze + CI recovery | authority artifacts under `docs/authority/` | [`foundry-v1.5-refreeze/engineer-feedback-rereview.md`](foundry-v1.5-refreeze/engineer-feedback-rereview.md) — **APPROVED** (T-1…T-4 closed; 0/0/0/3) | [`foundry-v1.5-refreeze/auditor-feedback.md`](foundry-v1.5-refreeze/auditor-feedback.md) — **APPROVED** (0/0/0/3) on subject `0d578bfa…c41df` | operator-accepted and landed (`22e5e00f`); M-1 carried forward as the binding pre-Sprint-3 condition |
| `m1-l3-l4-provenance-hardening` | Pre-Sprint-3 provenance-tooling hardening — close M-1 / L-3 / L-4 | [`m1-l3-l4-provenance-hardening/reviewer.md`](m1-l3-l4-provenance-hardening/reviewer.md) | [`m1-l3-l4-provenance-hardening/engineer-feedback.md`](m1-l3-l4-provenance-hardening/engineer-feedback.md) — **APPROVED** (0/0/0/4) on subject `f75e4dbc…27fa9a`; M-1/L-3/L-4 independently confirmed closed, freshness control assessed sufficient on every authoritative path | — | **REVIEW_APPROVED**, awaiting exact-tree audit of subject `f75e4dbc…27fa9a`; uncommitted, `HEAD == 22e5e00f` |

**Superseded 2026-08-13.** Both pre-Sprint-3 conditions closed and landed: the M-1/L-3/L-4
provenance-hardening node at `26ca4cd6` and the adaptive-routing reconciliation package at
`bc5dedc2`, the latter being `master == origin/master` at Sprint-3 node start. `26ca4cd6` is an
ancestor of `HEAD`, so the block above is discharged and `/implement sprint-3` was authorized.

## `m1-l3-l4-provenance-hardening` artifacts

| file | role |
|---|---|
| `m1-l3-l4-provenance-hardening/reviewer.md` | implementation report — prior findings consumed, live pre-fix reproduction of M-1, the two-half source universe and why it is extension-independent, 15-probe negative results with per-probe fence attribution, the L-3 control's own mutation test, preservation evidence (6 accepted parity digests re-derived), and the 6-file subject manifest |

Trajectory logs live under `trajectory/`. Review and audit artifacts
(`engineer-feedback.md`, `auditor-sprint-feedback.md`) are written by
`/review-sprint` and `/audit-sprint` — not by `/implement`.
