COMPLETED at 2026-08-15T00:00:00Z by /audit-sprint sprint-6 re-audit approval (Sprint 6 audit-remediation re-audit APPROVED — 0 critical / 0 high / 0 medium / 6 low; auditor-sprint-feedback-2.md, superseding the CHANGES_REQUIRED of auditor-sprint-feedback.md). Exact-tree identity re-derived from git at re-audit entry AND exit, unchanged: Group A fingerprint 01921956f5d6fd243197dc605af0f7c84fbe276f94df347fa3a5a72b67ede489, 61 files; Group B activated authority 1e6515cc1c79f553c68d8172e16c78c183133ad0bc6057bf07cfb400ea267a2c byte-identical throughout; baseline 92f8762111cd89c4cbdd4bcb11d06bf368f29377, zero commits ahead, uncommitted. Both lockfiles byte-identical to the review-era manifest, so no dependency moved during remediation; census unchanged (indexer 3 direct, web 6 direct + 1 dev, all exact). All six prior audit MEDIUMs verified CLOSED on independent evidence, not inherited: M-1 cause consumption confirmed by 12 adversarial probes (3-burns/1-cause, 2-causes/2-burns, cause-with-no-burn, cross-type 1:1 with ambiguity still raised, reorder determinism, mint-side consumption) plus a reproduced mutation killing exactly 1/13; M-2 verified by opening the live PGlite database the delivered app wrote and rejecting direct writes against the REAL tables by constraint name (legs_sum/cause_domain/class_is_never_backing/direction_domain all 23514, occurrence uniqueness 23505 on a real CREATE UNIQUE INDEX), with a valid row still accepted so nothing is vacuous, and the omitted ref_epoch_id FK affirmed correct because CHECK/unique never fire on DELETE while a FK would constrain rollback deletion order; M-3 workflow inspected for bypasses (no continue-on-error, no || true, no if:, no path filters, no new GitHub Action, runner Node asserted >=20) and its exact command sequence replayed locally including the pin negative-demonstration (next 15.1.12->15.1.11 yields VERDICT: FAIL, restore yields PASS); M-4 exhaustive search finds zero npx/npm exec/curl/wget/bunx/pnpm dlx, all spawns process.execPath into local node_modules; M-5 ignore-scripts=true effective in both roots with no overrides anywhere and CI restating --ignore-scripts; M-6 both layers proven independently, the send-time layer by driving the pinned viem directly (wrong chain: NO eth_sendTransaction issued; accepted chain: sent). Evidence re-derived in-node: forge 397/397, indexer 24/24, web units 10/10, Playwright 46/46, live independent reconstruction PASS with 0 ambiguities, ponder runtime gate PASS, accepted-pins/codegen/static-export/bundled-RSC gates PASS. Six LOW residuals carried: L-5 re-graded with a CORRECTION to the prior audit's reachability claim — absolute-form request targets do escape the prefix check (PoC: canary outside out/ served 200), bounded to web/out* siblings which hold only build output, plus a confirmed 0.0.0.0 bind logged as 127.0.0.1; L-2 CI portion verified retired (production build precedes the gates) with the local path still live (web/out currently holds a test build); L-1 API input validation, L-3 read->click hardening, L-4 codegen wording, L-6 new informational (constraint self-test requires CREATE TEMP TABLE privilege at startup; constraints applied from Rig:setup). No implementation bytes mutated by audit: one canary file and two source mutations created, used, and restored with hash verification. NOT operator-accepted, not committed, not landed, ledger not flipped.
